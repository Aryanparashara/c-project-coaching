/*#include<stdio.h>
void main(void)
{
  int num,sum=0,rem;
  printf("\n Enter a number ");
  scanf("%d",&num);
  // keep diving until the number is not zero 
  while(num!=0)
  {
    rem=num%10;
    sum=sum+rem;
    num=num/10;
  }
  printf("\n sum of digits of the numbers is %d",sum);
}*/
